export enum EventNames {
  openModal = 'openModal',
  hideModal = 'hideModal',
}

export enum ModalNames {
  auth = 'auth',
  confirmation = 'confirmation',
}

export enum AuthViews {
  login,
  createAccount,
  forgotPassword,
}
